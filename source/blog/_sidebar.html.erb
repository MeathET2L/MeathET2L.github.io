<aside class="large-3 columns right">
  <h3>Recent Articles</h3>
  <panel>
    <% blog.articles[0...5].each do |article| %>
      <%= link_to article.title, article %><br><br>
    <% end %>
  </panel>
  <h3>Topics</h3>
    <% blog.tags.each do |tag, articles| %>
        <%= link_to "#{tag} (#{articles.size})", tag_path(tag), :class => 'tiny button info round' %>
    <% end %>

  <h3>View All</h3>
  <panel>
    <% blog.articles.group_by {|a| a.date.year }.each do |year, articles| %>
      <%= link_to "#{year} (#{articles.size})", blog_year_path(year) %><br><br>
    <% end %>
  </panel>
</sidebar>
